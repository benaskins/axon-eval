name: autotune-122b
model: /Users/benaskins/models/mlx/Qwen3.5-122B-A10B-5bit
server: vllm-mlx
server_args: --enable-auto-tool-choice --tool-call-parser qwen3_coder --reasoning-parser qwen3
command: claude -p "Read autotune/mlx-program.md and follow the instructions exactly. Begin the autotune experiment loop. Today's date is $(date +%Y-%m-%d). IMPORTANT: set TARGET_THROUGHPUT=0.50 in your first config edit before establishing the baseline. The current target of 0.15 is too low and gives a perfect score immediately. Also explore MAX_NUM_SEQS and EVAL_WORKERS together (2, 4, 8) to find the throughput sweet spot." --allowedTools "Bash,Read,Write,Edit,Grep,Glob"
workdir: /Users/benaskins/dev/lamina/axon-eval
priority: 10
